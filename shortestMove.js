/**
 * A knight can get from any square of the board to any other
 * in a maximum of 6 moves, therefore check if it can be done in
 * x moves, with x increasing from 0 to 6.
 */
/**
 * Build a binary search tree using the adjacency list
 * for every node, the valid moves have an x and y coordinate
 * There can be between 2 and 8 valid moves for a knight
 * This means no binary tree - they can only decide between two options
 * it would be very expensive to build a list of the optimal path from
 * each node to every other node (64 factorial??)
 */
